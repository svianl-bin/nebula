// 该文件主要作用是为了简化全局组件中的类型导入路径

import type { ECOption } from '@components/ECharts/echarts'
import type { TypedInstanceRef } from '@/components/TypedText/typed'

export type ECOption = ECOption

export type TypedInstanceRef = TypedInstanceRef
